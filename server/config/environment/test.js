'use strict';

// Test specific configuration
// ===========================
module.exports = {
    // Firebase connection options
    firebase: {
        databaseURL: "https://cloudanalyser.firebaseio.com",
        serviceAccount: "cloudanalyser/test.json"
    }
};